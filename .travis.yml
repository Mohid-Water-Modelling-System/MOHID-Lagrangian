language: generic
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gfortran-7
before_script:
  - cmake --version
  - export FC=/usr/local/bin/gfortran
  - FC --version
  - cd Libs  
  - ./MakeLibraries.sh
  - cd ..
  - ./MakeMOHIDLagrangian.sh
