language: generic
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gfortran-8
before_script:
  #- cmake --version
  - sudo ln -fs /usr/bin/gfortran-8 /usr/bin/gfortran && gfortran --version
  - export FC=gfortran
  - cd Libs  
  - ./MakeLibraries.sh
  - cd ..
  - ./MakeMOHIDLagrangian.sh
